import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Requests",
};

const RequestPage = () => {
  return <div className="text-black p-4">Request Page</div>;
};

export default RequestPage;
